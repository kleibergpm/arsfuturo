import express from 'express'; import cors from 'cors'; import swaggerUi from 'swagger-ui-express'; import swaggerJsdoc from 'swagger-jsdoc';
import routes from './routes/index.js'; import { env } from './config/env.js'; import { errorHandler, notFound } from './middleware/errors.js';
const app = express(); const spec = swaggerJsdoc({ definition: { openapi: '3.0.3', info: { title: 'ARS Futuro API', version: '1.0.0', description: 'API MVC para gestión de ARS' }, components: { securitySchemes: { bearerAuth: { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' } } }, paths: { '/api/auth/login': { post: { summary: 'Inicia sesión', requestBody: { required: true }, responses: { 200: { description: 'JWT y usuario' }, 401: { description: 'Credenciales inválidas' } } } }, '/api/dashboard/resumen': { get: { summary: 'Obtiene KPIs', security: [{ bearerAuth: [] }], responses: { 200: { description: 'Resumen del dashboard' } } } } } }, apis: [] });
app.use(cors({ origin: env.corsOrigin })); app.use(express.json()); app.use((req, res, next) => { console.info(`${req.method} ${req.path}`); next(); }); app.get('/health', (req,res) => res.json({ status: 'ok' })); app.use('/api/docs', swaggerUi.serve, swaggerUi.setup(spec)); app.use('/api', routes); app.use(notFound); app.use(errorHandler); export default app;
